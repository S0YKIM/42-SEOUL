# Base Image (The penultimate stable version Debian 10)
FROM debian:buster

# Port Number
EXPOSE 3306

# Package Installation
RUN apt-get update && apt-get install -y \
	mairadb-server \
	vim \
	procps

# TODO: COPY
# TODO: ENTRYPOINT
# TODO: CMD
